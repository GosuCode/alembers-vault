# Supabase (public — safe for client bundles)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Supabase (server-only — never expose to the client)
# Used by server endpoints in src/pages/api/ for privileged operations.
SUPABASE_SERVICE_ROLE_KEY=
